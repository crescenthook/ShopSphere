server:
  port: 8083

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://postgres:5432/product_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
  instance:
    hostname: auth-service

logging:
  level:
    org.springframework.security: DEBUG

jwt:
  secret: my-super-secret-key-for-shopshere-jwt-token-generation
  expiration: 3600000